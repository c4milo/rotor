//! The cost probes that fill docs/costs.md: one probe per row, each reporting its median and its
//! p99 in nanoseconds with how it measured. Run with `zig build bench-costs`. Not written yet.
pub fn main() void {}
